# proxy_cache_path /tmp/nginx/PROXY_CACHE levels=1:2 keys_zone=PROXY_CACHE:256m inactive=1y;
# fastcgi_cache_path /tmp/nginx/WP_CACHE levels=1:2 keys_zone=WP_CACHE:256m inactive=1y;

server {
  server_name www.example.com example.com;
  
  include conf.d/common.conf;
  include conf.d/ssl.conf;
  include conf.d/logs.conf;

  ssl_certificate /etc/letsencrypt/live/example.com/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/example.com/privkey.pem;
  ssl_trusted_certificate /etc/letsencrypt/live/example.com/chain.pem;

  root /var/www/example.com;
  

  # location / {
  #   include conf.d/proxy.conf;
  #   include conf.d/proxycache.conf;
  #   proxy_cache PROXY_CACHE;
  #   proxy_pass http://127.0.0.1:8080;
  # }


  # include conf.d/wordpress.conf;
  # set $php_root /var/www/html;
  #  
  # location ~ \.php$ {
  #   include conf.d/php.conf;
  #   include conf.d/phpcache.conf;
  #   fastcgi_cache WP_CACHE;
  #   fastcgi_pass 127.0.0.1:9000;
  # }
}
